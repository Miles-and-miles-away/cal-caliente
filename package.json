{
  "name": "cal-caliente-flutter-tooling",
  "private": true,
  "scripts": {
    "firebase": "firebase",
    "seed": "node scripts/seed/seed_emulator.js",
    "test:rules": "firebase emulators:exec --only firestore --project=demo-cal-caliente \"jest --config test/firestore/jest.config.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-admin": "^13.0.0",
    "firebase-tools": "^15.3.1",
    "jest": "^29.7.0"
  }
}
